* TODO
  * Rule brainstorming
    * Don't give all access by default? (special all key)
    * Positives take precedence (iterate over positives and check them first)
    * Use +@ or -@ for read/write all/none
    * Potential thread safety issues with user profiles. Could add a lock to each user for this, necessary?
  * Fix logger with files vs stdout
  * Fix new TTL commands (EXPIREAT, EXPIRETIME)
  * Implement ArgString, ArgInt, etc on server.

* Backlog
  * Add logging to all handler errors
  * Make save / load interface nicer
  * Handle TCP disconnect gracefully (call disconnect info function)

* Way backlog
  * Different database numbers
  * Add help command
  * Improve testing environment
  * Github actions testing
  * Pipelining
  * Blocking pop calls
  * Include length calculations in RESP data
