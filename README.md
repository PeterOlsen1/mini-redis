# mini-redis

A little recreation of redis I made to pracitce go and system design.

### to use

Two terminals are necessary, one to run `go run ./server` and one to run `go run ./cli`

You can also use a client directly from go